[package]
name = "gen-shrincs-args"
version = "0.1.0"
edition = "2021"

# Host-side test-vector generator. It needs `std` because signing requires
# getrandom/threading and only runs on the native build host.
[dependencies]
shrincs-rs = { path = "../../../.." }
